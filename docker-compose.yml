# docker-compose.yml — Full system (đặt ở root: /URL-Shortener-System/)
# Chạy: docker-compose up --build

version: '3.8'
services:

  # ── Infrastructure ──────────────────────────────────────────

  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      - ACCEPT_EULA=Y
      - SA_PASSWORD=23092005@Bao
    ports:
      - "1433:1433"
    healthcheck:
      test: ["CMD-SHELL", "/opt/mssql-tools18/bin/sqlcmd -S localhost -U sa -P '23092005@Bao' -Q 'SELECT 1' -No -C || exit 1"]
      interval: 10s
      retries: 10
      start_period: 30s
      timeout: 5s

  redis:
    image: redis:latest
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management
    ports:
      - "5672:5672"
      - "15672:15672"

  # ── Microservices ───────────────────────────────────────────

  userservice:
    build:
      context: .
      dockerfile: src/Services/UserService/Dockerfile
    ports:
      - "5001:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=UserDb;User Id=sa;Password=23092005@Bao;TrustServerCertificate=True;Encrypt=False
      - JwtSettings__Key=SuperSecretKey_AtLeast32Characters_ForHS256!
      - JwtSettings__Issuer=UserService
      - JwtSettings__Audience=URLShortenerApp
      - JwtSettings__ExpiresInHours=168
      - RabbitMQ__Host=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
      - Services__URLService=http://urlservice:8080
    depends_on:
      sqlserver:
        condition: service_healthy
      rabbitmq:
        condition: service_started

  urlservice:
    build:
      context: .
      dockerfile: src/Services/URLService/Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=UrlDb;User Id=sa;Password=23092005@Bao;TrustServerCertificate=True;Encrypt=False
      - Redis__ConnectionString=redis:6379
      - JwtSettings__Key=SuperSecretKey_AtLeast32Characters_ForHS256!
      - JwtSettings__Issuer=UserService
      - JwtSettings__Audience=URLShortenerApp
      - RabbitMQ__Host=rabbitmq
      - RabbitMQ__Username=guest
      - RabbitMQ__Password=guest
    depends_on:
      sqlserver:
        condition: service_healthy
      redis:
        condition: service_started
      rabbitmq:
        condition: service_started